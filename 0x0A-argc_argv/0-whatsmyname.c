#include <stdio.h>
/**
 * main - It takes 2 arguments
 * @argc: Represents number of parameters
 * @argv: pointer to arrays of strings
 * Return: returns 0
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
