#include <stdio.h>

/**
 * main - entry point
 * Description: Prints out the alphabet in lowercase and in uppercase
 * Return: returns 0 if successful
 */
int main(void)
{
char ph, ph1;

for (ph = 'a'; ph <= 'z'; ph++)
{
putchar(ph);
}
for (ph1 = 'A'; ph2 <= 'Z'; ph++)
{
putchar(ph1);
}
putchar('\n');
return (0);
}